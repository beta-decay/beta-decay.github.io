botData = [
	{
		name: "Test1",
		func: function(x, y, colour, grid, bots) {
			move = ["up","down","right","left","none"][Math.floor(Math.random()*4)]
			return "pd:"+move;
		}
	},
	{
		name: "Test2",
		func: function(x, y, colour, grid, bots) {
			move = ["up","down","right","left","none"][Math.floor(Math.random()*4)]
			return "pd:"+move;
		}
	},
	{
		name: "Test3",
		func: function(x, y, colour, grid, bots) {
			move = ["up","down","right","left","none"][Math.floor(Math.random()*4)]
			return "pd:"+move;
		}
	},
	{
		name: "Test4",
		func: function(x, y, colour, grid, bots) {
			move = ["up","down","right","left","none"][Math.floor(Math.random()*4)]
			return "pd:"+move;
		}
	},
	{
		name: "Test5",
		func: function(x, y, colour, grid, bots) {
			move = ["up","down","right","left","none"][Math.floor(Math.random()*4)]
			return "pd:"+move;
		}
	},
	{
		name: "Test6",
		func: function(x, y, colour, grid, bots) {
			move = ["up","down","right","left","none"][Math.floor(Math.random()*4)]
			return "pd:"+move;
		}
	},
	{
		name: "Test7",
		func: function(x, y, colour, grid, bots) {
			move = ["up","down","right","left","none"][Math.floor(Math.random()*4)]
			return "pd:"+move;
		}
	},
	{
		name: "Test8",
		func: function(x, y, colour, grid, bots) {
			move = ["up","down","none"][Math.floor(Math.random()*4)]
			return "pd:"+move;
		}
	},
	{
		name: "Test9",
		func: function(x, y, colour, grid, bots) {
			move = ["left","none"][Math.floor(Math.random()*4)]
			return "pd:"+move;
		}
	},
	{
		name: "Test10",
		func: function(x, y, colour, grid, bots) {
			move = ["right","none"][Math.floor(Math.random()*4)]
			return "pd:"+move;
		}
	}
]